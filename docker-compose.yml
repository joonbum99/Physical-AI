version: '3.8'

services:
  rpi-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: rpi-arm64-dev:latest
    container_name: rpi-arm64-container
    platform: linux/arm64
    ports:
      - "2222:22"
    volumes:
      - ./app:/workspace
    restart: unless-stopped
